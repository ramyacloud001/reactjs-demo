pipeline {
     agent any
     stages {
        stage("Build") {
            steps {
                sh 'build.sh'
            }
        }
        stage("Deploy") {
            steps {
                sh '/var/www/jenkins-react-app/deploy.sh'
            }
        }
    }
}
