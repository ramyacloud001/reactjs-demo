pipeline {
     agent any
     stages {
        stage("Build") {
            steps {
                sh "chmod +x -R ${env.WORKSPACE}"
                sh './jenkins/build.sh'
            }
        }
        stage("Deploy") {
            steps {
                sh '/var/www/jenkins-react-app/deploy.sh'
            }
        }
    }
}
