pipeline {
     agent {label 'ubuntu'}
     stages {
        stage("Build") {
            steps {
                sh '/home/ubuntu/build.sh'
            }
        }
        stage("Deploy") {
            steps {
                sh '/var/www/jenkins-react-app/deploy.sh'
            }
        }
    }
}
