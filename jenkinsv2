pipeline {
     agent any
     stages {
        stage("Build") {
            steps {
                sh 'sudo ./build.sh'
            }
        }
        stage("Deploy") {
            steps {
                sh '/var/www/jenkins-react-app/deploy.sh'
            }
        }
    }
}
